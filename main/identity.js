define(

    /**
     * A function which returns its first argument
     *
     * @param {*} a
     */
   function identity(a){
      return a;
   }

// Naga.multipleIdentity = Naga.argumentsAsList( Naga.identity );
);